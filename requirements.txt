# Full developer environment. Minimal consumers install .[fetch] or .[api].
# Dependency definitions live in pyproject.toml.
des-contracts @ git+https://github.com/TobiasEdman/des-contracts.git@v0.1.0
-e .[training,api,viz]
