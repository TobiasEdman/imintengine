# Core
numpy>=1.24
scipy>=1.10
Pillow>=10.0
matplotlib>=3.7
pyyaml>=6.0

# Geospatial
rasterio>=1.3
shapely>=2.0
geopandas>=0.14
fiona>=1.9
pyproj>=3.6
pyarrow>=14         # geopandas .read_parquet engine for LPIS spatial parquets

# Deep Learning (training)
torch>=2.1
timm>=0.9
einops>=0.7
huggingface_hub>=0.20

# Image processing
opencv-python-headless>=4.8
scikit-image>=0.22  # superpixel segmentation (imint/inference/superpixel_refine.py)

# System monitoring (dashboard)
psutil>=5.9

# Data processing
pandas>=2.0
openpyxl>=3.1   # pandas .read_excel engine for the SLU NFI plot xlsx (imint/training/slu_nfi.py)

# Satellite data (needed for data fetching via DES)
openeo>=0.20
requests>=2.31

# Atmosphere / ERA5 — used by imint/training/era5_aux.py and
# imint/training/optimal_fetch.py. xarray is needed for both the SCL netCDF
# screening and the polytope fallback; cftime is the transitive xarray
# dependency that pip won't pull on its own and whose absence presents as a
# misleading "did not find a match in any of xarray's currently installed
# IO backends" error when xarray's auto-decoder tries to read CDS-style
# NetCDFs. See DigitalEarthSweden/space-datalab-metafilter PR #2 and
# ~/Developer/agentic_workflow/docs/lessons/import_time_credential_validation.md
# for the documented gotcha.
xarray>=2024.7
cftime>=1.6.4

# cdsapi (era5_aux._fetch_via_cdsapi fallback) and polytope-client
# (era5_aux primary fetcher) are lazy-imported inside their respective
# functions, so they're optional at install time — install them only when
# you actually need to fetch via CDS or ECMWF Polytope:
#   pip install cdsapi
#   pip install polytope-client

# Visualization helpers
svgpath2mpl>=0.2

# Notebook development (optional)
# jupyter>=1.0
# ipykernel>=6.0

# Shared contracts — 23-class schema, Analyzer base, KG JSON Schema,
# embedding config. Extracted from this repo in 2026-04 (rollout W3.6).
des-contracts @ git+https://github.com/TobiasEdman/des-contracts.git@v0.1.0

# HTTP API (W4.1). Optional in dev; required in the deployed Docker
# image. Pinned tight — fastapi 0.x has occasional breaking minor bumps.
fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<1
gunicorn>=22,<24
httpx>=0.27,<1   # used by /analyze callback hook + by tests

# SLA telemetry (W4.5). Optional — telemetry module is fail-open when
# this isn't installed; metrics endpoint still serves a placeholder.
prometheus-client>=0.20,<1

# Super-resolution showcase (imint/analyzers/sr/). Optional in dev —
# each wrapper raises a clear error if its package is missing, so the
# showcase still renders the models that are available. Pinned loose
# until the ESA OpenSR stack stabilises on PyPI.
# - opensr-utils:  production wrapper (sliding-window, edge-blend)
# - opensr-model:  LDSR-S2 latent diffusion
# - opensr-test:   benchmark (consistency / synthesis / hallucination)
opensr-utils; extra == "sr"
opensr-model; extra == "sr"
opensr-test;  extra == "sr"
