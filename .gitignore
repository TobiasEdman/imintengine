# Outputs and data — never commit these
outputs/*
# But keep the showcase (small, deliverable)
!outputs/imint_showcase.html
!outputs/era5_metafilter_showcase.html
!outputs/showcase/
!outputs/showcase/**
data/
!docs/data/
*.npy
*.tif
*.geotiff
*.pt
*.pth
*.ckpt
*.h5
*.tar.gz
*.npz

# Exception: track small pre-trained model weights needed for
# out-of-the-box notebook execution. pib-ml-grazing is 540 KB, MIT.
!imint/fm/pib_grazing/**/*.pth

# Baselines (large numpy files)
**/baselines/

# Cached CDN libraries for offline HTML reports
.lib_cache/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# macOS
.DS_Store

# Credentials & endpoints — never commit
.cdsapirc
utils/config.py
/config.py
*credentials*
*secret*
!imint/config/secrets.py
*.env
.env
.env.*
.des_token
.des_token.bak
ssh-copy-id*
.skg_endpoints

# M1 Max (ColonyOS worker)
# SSH: tobiasedm@192.168.50.100

# WordPress (Digital Earth Sweden)
# Admin: https://digitalearth.se/wp-login.php
# IMINT showcase iframe height needs to be increased when adding new tabs/panels

# Environment configs (secrets) — keep template.env only
!config/environments/template.env
config/environments/dev.env
config/environments/test.env
config/environments/prod.env

# IDE
.vscode/
.idea/

# Run artifacts (training/eval logs, dashboards, sandbox dirs) — never commit
/training_log.json
/training_raw.log
/training_dashboard.html
/class_stats.json
/confusion_matrix.png
/system_metrics.json
/fetch_status.json
/tile_*.png
/tile_*.html
/tile_previews.json
/hygge_tile_viz.png
/tile_comparison.png
dashboards/live_logs/
dashboards/pixel_live/tile_preds/
dashboards/pixel_live/training_log.json
dashboards/pixel_training_dashboard.html
/static/
/viewer_tiles/
docs/*.bak
docs/tile_inspect.html

# Trivial one-shot kubectl helper jobs — easy to recreate, no need to track
k8s/lpis-ls-job.yaml
k8s/lpis-viz-pull-job.yaml
k8s/lpis-viz-server.yaml

# Tooling caches
.claude/
scripts/verify_fetch_refactor.py
