# Training scope
Training, datasets, evaluation execution and tile campaigns live here.
Import shared schema from imint.schema, provider access from imint.data and numerical metrics from imint.metrics.
Keep training/evaluation in this repository for now: model loaders, transforms and checkpoint compatibility change together.
Own jobs and mutable dataset paths explicitly; Git worktrees do not isolate cluster storage.
