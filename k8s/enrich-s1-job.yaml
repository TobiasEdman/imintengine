apiVersion: batch/v1
kind: Job
metadata:
  name: enrich-s1
  namespace: prithvi-training-default
spec:
  backoffLimit: 4
  activeDeadlineSeconds: 172800  # 48h
  template:
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  # srv04/srv06 = runc pod-sandbox bug + observed OOM host
                  - key: kubernetes.io/hostname
                    operator: NotIn
                    values: ["p02r09srv04", "p02r09srv06"]
      containers:
        - name: enricher
          image: python:3.11-slim
          command:
            - /bin/bash
            - -c
            - |
              set -e
              export PYTHONUNBUFFERED=1
              echo "=== S1 SAR Enrichment ==="

              apt-get update -qq && apt-get install -y -qq git libgdal-dev gdal-bin > /dev/null 2>&1
              pip install --quiet --no-cache-dir numpy scipy rasterio requests pyproj pyyaml Pillow httpx pystac-client planetary-computer

              mkdir -p /workspace && cd /workspace
              git clone --depth 1 https://github.com/TobiasEdman/ImintEngine.git imintengine
              cd /workspace/imintengine
              pip install --no-cache-dir -e . --no-deps 2>/dev/null || true

              # S1_BACKENDS=mpc: MPC is the verified working path (WarpedVRT
              # over the rotated GRD swath, σ⁰ 100% valid px — verify-s1-fix
              # 2026-08-04). CDSE download 401s (needs a password-grant token,
              # not the client_credentials account — follow-up); skipping it
              # avoids a wasted search+401 per tile. MPC is auth-free and not
              # WAF-limited, so concurrency is fine again.
              # --workers 2: WarpedVRT over a ~250 km GRD swath is memory-
              # heavy; 4 workers OOMKilled at 8 Gi (2026-08-04, after 5885
              # real hits). 2 workers + 24 Gi holds. skip-existing means a
              # restart continues from where the OOM left off.
              python scripts/enrich_tiles_s1.py \
                --data-dir /data/unified_v2_512 \
                --s1-backend cascade \
                --workers 2 \
                --skip-existing

          env:
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: S1_BACKENDS
              value: "mpc"
            - name: CDSE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: cdse-credentials
                  key: client_id
                  optional: true
            - name: CDSE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: cdse-credentials
                  key: client_secret
                  optional: true
          resources:
            requests:
              cpu: "4"
              memory: "24Gi"
            limits:
              cpu: "4"
              memory: "24Gi"
          volumeMounts:
            - name: training-data
              mountPath: /data
      volumes:
        - name: training-data
          persistentVolumeClaim:
            claimName: training-data-cephfs
