# GENERATED by scripts/gen_ladder_manifests.py — do not edit.
# ICE-resident watchdog for the LUCAS split verification run. It
# owns only one restricted durable state subPath and the narrow
# Kubernetes reads/CAS updates required by the freeze protocol.
# It intentionally retains API egress and has no TTL.
# Plan: docs/experiments/ladder_distill_stage.md
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ladder-crop-source-freeze-operator
  namespace: prithvi-training-default
  labels: { app: unified-training, purpose: ladder-crop-source-freeze-operator }
automountServiceAccountToken: true
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ladder-crop-source-freeze-operator
  namespace: prithvi-training-default
  labels: { app: unified-training, purpose: ladder-crop-source-freeze-operator }
rules:
  - apiGroups: [""]
    resources: ["pods", "replicationcontrollers"]
    verbs: ["get", "list"]
  - apiGroups: [""]
    resources: ["configmaps"]
    resourceNames: ["crop-source-freeze-lease"]
    verbs: ["get", "update"]
  - apiGroups: ["batch"]
    resources: ["jobs", "cronjobs"]
    verbs: ["get", "list"]
  - apiGroups: ["batch"]
    resources: ["cronjobs"]
    resourceNames: ["ladder-queue", "gpu-reaper", "campaign-orchestrator"]
    verbs: ["update"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "daemonsets", "replicasets"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ladder-crop-source-freeze-operator
  namespace: prithvi-training-default
  labels: { app: unified-training, purpose: ladder-crop-source-freeze-operator }
subjects:
  - kind: ServiceAccount
    name: ladder-crop-source-freeze-operator
    namespace: prithvi-training-default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: ladder-crop-source-freeze-operator
---
apiVersion: batch/v1
kind: Job
metadata:
  name: ladder-crop-source-freeze-operator
  namespace: prithvi-training-default
  labels: { app: unified-training, purpose: ladder-crop-source-freeze-operator }
spec:
  backoffLimit: 6
  template:
    metadata:
      labels: { app: unified-training, purpose: ladder-crop-source-freeze-operator }
    spec:
      activeDeadlineSeconds: 43200
      automountServiceAccountToken: true
      serviceAccountName: ladder-crop-source-freeze-operator
      restartPolicy: OnFailure
      imagePullSecrets:
        - name: ghcr-push
      securityContext:
        seccompProfile: { type: RuntimeDefault }
      initContainers:
        - name: prepare-state
          image: ghcr.io/tobiasedman/imint-ladder-crop-distill@sha256:5e8548af0b359276c2dc411f61695d3c8c46c9d41ac22cf4fe74d2adf88db6a5
          imagePullPolicy: IfNotPresent
          command:
            - /usr/local/bin/python
          args:
            - /opt/imintengine/scripts/crop_source_freeze_operator.py
            - prepare
            - --state-parent
            - /state-parent
          env:
            - name: CROP_DISTILL_SOURCE_GIT_SHA
              value: "760eec58838de10586aa8151fb3ad07d07f40adb"
            - name: CROP_DISTILL_IMAGE
              value: "ghcr.io/tobiasedman/imint-ladder-crop-distill@sha256:5e8548af0b359276c2dc411f61695d3c8c46c9d41ac22cf4fe74d2adf88db6a5"
            - name: POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
              add: ["CHOWN", "FOWNER"]
            readOnlyRootFilesystem: true
            runAsUser: 0
            runAsGroup: 2000
          resources:
            requests: { cpu: "500m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "256Mi" }
          volumeMounts:
            - name: training-data-cephfs
              mountPath: /state-parent
              subPath: ops/crop-distill/source-access
      containers:
        - name: freeze-operator
          image: ghcr.io/tobiasedman/imint-ladder-crop-distill@sha256:5e8548af0b359276c2dc411f61695d3c8c46c9d41ac22cf4fe74d2adf88db6a5
          imagePullPolicy: IfNotPresent
          command:
            - /usr/local/bin/python
          args:
            - /opt/imintengine/scripts/crop_source_freeze_operator.py
            - serve
            - --state-dir
            - /state
            - --run-id
            - lucas-crop-attempt-14-verify
            - --namespace
            - prithvi-training-default
          env:
            - name: CROP_DISTILL_SOURCE_GIT_SHA
              value: "760eec58838de10586aa8151fb3ad07d07f40adb"
            - name: CROP_DISTILL_IMAGE
              value: "ghcr.io/tobiasedman/imint-ladder-crop-distill@sha256:5e8548af0b359276c2dc411f61695d3c8c46c9d41ac22cf4fe74d2adf88db6a5"
            - name: POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: HOME
              value: /tmp
            - name: TMPDIR
              value: /tmp
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 2000
            runAsGroup: 2000
          resources:
            requests: { cpu: "500m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "256Mi" }
          volumeMounts:
            - name: training-data-cephfs
              mountPath: /state
              subPath: ops/crop-distill/source-access/crop-source-freeze
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: training-data-cephfs
          persistentVolumeClaim: { claimName: training-data-cephfs }
        - name: tmp
          emptyDir:
            sizeLimit: 128Mi
