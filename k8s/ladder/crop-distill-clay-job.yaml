# GENERATED by scripts/gen_ladder_manifests.py — do not edit.
# LUCAS crop-distill stage for clay: r2 checkpoint → features at
# the frozen LUCAS crop distill points → pinned-protocol crop OOF
# (clay_r2_crop_distillability.json). Evidence for the R5
# decision — writes NO gate, feeds NO queue rung.
# Prereq: ladder-lucas-crop-split (once, shared).
# Plan: docs/experiments/ladder_distill_stage.md
apiVersion: batch/v1
kind: Job
metadata:
  name: ladder-crop-distill-clay
  namespace: prithvi-training-default
  labels: { app: unified-training, purpose: ladder-crop-distill, model: clay }
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 172800
  template:
    metadata:
      labels: { app: unified-training, purpose: ladder-crop-distill, model: clay }
    spec:
      activeDeadlineSeconds: 43200
      automountServiceAccountToken: false
      restartPolicy: Never
      imagePullSecrets:
        - name: ghcr-push
      securityContext:
        runAsNonRoot: true
        runAsUser: 2001
        runAsGroup: 2000
        seccompProfile: { type: RuntimeDefault }
      nodeSelector:
        accelerator: nvidia-gtx-2080ti
      containers:
        - name: crop-distill
          image: ghcr.io/tobiasedman/imint-ladder-crop-distill@sha256:25919e5cc0bc5ccba770b80f5906d0721cf47a779e3b4591a07cf5c5f3dc3599
          imagePullPolicy: IfNotPresent
          command:
            - /usr/local/bin/python
          args:
            - /opt/imintengine/scripts/run_crop_distill_job.py
            - --model
            - clay
          env:
            - name: CROP_DISTILL_SOURCE_GIT_SHA
              value: "7d935b9a0210f9a88b641ef24e7d83206c2fdcb0"
            - name: CROP_DISTILL_IMAGE
              value: "ghcr.io/tobiasedman/imint-ladder-crop-distill@sha256:25919e5cc0bc5ccba770b80f5906d0721cf47a779e3b4591a07cf5c5f3dc3599"
            - name: CROP_DISTILL_SPLIT_MANIFEST_SHA256
              value: "0d4e21d01a87de203eaa04408a11210e193dabbfa12bd440cdf0659bfc6179ed"
            - name: CROP_DISTILL_SPLIT_SOURCE_GIT_SHA
              value: "c6ad69242e7239662461bf7ff0b6bcd4d072509a"
            - name: HOME
              value: /work/home
            - name: TMPDIR
              value: /work/tmp
            # torch >= 2.10's inductor resolves a cache dir via
            # getpass.getuser(), which falls back to pwd.getpwuid() when no
            # LOGNAME/USER env exists — and the numeric runtime UIDs have no
            # passwd entry in the image (KeyError killed the seventh run at
            # extract-features). getpass reads these envs FIRST; the explicit
            # cache dir under the writable workdir is belt and braces.
            - name: LOGNAME
              value: crop-distill
            - name: USER
              value: crop-distill
            - name: TORCHINDUCTOR_CACHE_DIR
              value: /work/torch-inductor
            - name: POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 2001
            runAsGroup: 2000
          resources:
            requests: { cpu: "4", memory: "24Gi", ephemeral-storage: "8Gi", nvidia.com/gpu: "1" }
            limits: { cpu: "4", memory: "24Gi", ephemeral-storage: "8Gi", nvidia.com/gpu: "1" }
          volumeMounts:
            - name: training-data-cephfs
              mountPath: /cephfs/unified_v2_512
              subPath: unified_v2_512
              readOnly: true
            - name: training-data-cephfs
              mountPath: /cephfs/checkpoints/ladder/clay_r2
              subPath: checkpoints/ladder/clay_r2
              readOnly: true
            - name: training-data-cephfs
              mountPath: /cephfs/distill/crop_split
              subPath: distill/crop_split/crop_consumer
              readOnly: true
            - name: training-data-cephfs
              mountPath: /cephfs/crop-heads
              subPath: distill/crop_heads/clay_r2_crop_runs
            - name: training-data-cephfs
              mountPath: /cephfs/crop-records
              subPath: ops/crop-distill/clay
            - name: work
              mountPath: /work
      volumes:
        - name: training-data-cephfs
          persistentVolumeClaim: { claimName: training-data-cephfs }
        - name: work
          emptyDir:
            sizeLimit: 8Gi
