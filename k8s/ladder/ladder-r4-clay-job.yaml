# GENERATED by scripts/gen_ladder_manifests.py — do not edit.
# Ladder rung 4 (tradslag) for clay. Edit the base manifest
# (k8s/train-clay-job.yaml) or the generator, then regenerate.
# Plan: docs/experiments/label_source_ladder.md
# Clay v1.5 ensemble member — distill labels + Trädslag frac head.
#
# LAUNCH-READY as of 2026-08-14 — both prior blockers resolved. GO:
#   real-weight smoke on a 2080ti passed end to end (dataset → family_forward
#   → logits (2,28,H,W) + frac (2,4,H,W) → finite loss → backward).
#
#   A. WEIGHTS NOW CACHED. The loader requested the wrong filename
#      (clay-v1.5.ckpt at repo root → 404, leaving the .no_exist marker);
#      the real path is v1.5/clay-v1.5.ckpt (5.16 GB). Downloaded to
#      /data/model_cache/clay-v1.5.ckpt.
#   B. GLUE LANDED. The loaded object is a ClayMAE whose datacube-forward
#      Encoder is at .encoder; it must be loaded with mask_ratio=0 (else it
#      masks ~75% of patches) and fed a datacube dict with 4-wide
#      time+latlon. All handled in imint.fm.loaders.clay + clay_seg.
#
#   Coverage: 7882/7882 tiles are Clay-complete (spectral+b08+rededge, no
#   SAR needed). Clay is patch_size=8 → 504 crop OK (504/8=63). H100 has
#   ample memory; the smoke ran at 224px only to fit the 2080ti.
#   Also needs the claymodel pip pkg (git+Clay-foundation/model).
#
# HEAD (2026-08-14): multi-level UPerNet (ViTUPerNetHead), hooking 4 evenly-
#   spaced blocks of encoder.transformer.layers (24 blocks) — fairness parity
#   with Prithvi, not a linear probe.
# MEMORY (2026-08-14): request 48Gi (was 80Gi). Measured peak host tree-RSS
#   at bs=8/workers=16/504px = 7.5 GB (dataloader footprint); +CUDA host
#   overhead + margin → 48Gi lets all 3 FM jobs run in parallel under the
#   250Gi namespace quota (3×48=144 < ~195 available).
apiVersion: batch/v1
kind: Job
metadata:
  name: ladder-r4-clay
  namespace: prithvi-training-default
  labels: { app: unified-training, purpose: ladder, rung: "r4", model: clay }
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 172800
  template:
    metadata:
      labels: { app: unified-training, purpose: ladder, rung: "r4", model: clay }
    spec:
      activeDeadlineSeconds: 43200
      restartPolicy: Never
      nodeSelector: { accelerator: nvidia-h100 }
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - { key: kubernetes.io/hostname, operator: NotIn, values: ["p02r08srv02"] }
      containers:
      - name: trainer
        image: python:3.11-slim
        env:
        - { name: PYTHONUNBUFFERED, value: "1" }
        - { name: HF_HOME, value: "/cephfs/model_cache/hf" }
        command: ["/bin/bash","-c"]
        args:
        - |
          set -e
          export PYTHONUNBUFFERED=1
          echo "=== Clay v1.5 member — distill labels + frac head (28-class) — $(date) ==="
          apt-get update -qq && apt-get install -y -qq git > /dev/null 2>&1
          pip install --quiet --no-cache-dir torch torchvision --index-url https://download.pytorch.org/whl/cu121
          pip install --quiet --no-cache-dir timm numpy scipy scikit-learn huggingface_hub einops
          pip install --quiet --no-cache-dir "git+https://github.com/Clay-foundation/model.git"
          mkdir -p /workspace && cd /workspace
          BRANCH=agent/te/opus/nfi-nmd2023-benchmark
          git clone --depth 1 --branch "$BRANCH" \
            https://github.com/TobiasEdman/ImintEngine.git imintengine
          cd /workspace/imintengine
          pip install --no-cache-dir -e . || pip install --quiet --no-cache-dir -r requirements.txt
          echo "CLONED $BRANCH HEAD: $(git rev-parse --short HEAD)"
          # --- PREFLIGHT: fail loud unless weights + training glue are real ---
          python - <<'PY'
          import inspect, os, sys
          from imint.training import unified_dataset as ud
          from imint.fm import forward_router as fr
          from imint.fm.clay_seg import ClaySegmentationModel as C
          fails = []
          if not os.path.exists("/data/model_cache/clay-v1.5.ckpt"):
              fails.append("clay-v1.5.ckpt not present at /data/model_cache")
          if "clay_v1_5" not in ud.UnifiedDataset._SUPPORTED_MODEL_KEYS:
              fails.append("dataset cannot build clay tensors")
          if "model_keys" not in inspect.getsource(__import__('scripts.train_unified', fromlist=['x'])):
              fails.append("train_unified.py never sets model_keys")
          if "return_fractions" not in inspect.signature(C.forward).parameters:
              fails.append("ClaySeg.forward lacks return_fractions/frac head")
          # Per-family routing lives in forward_router.family_forward. Assert the
          # Clay branch routes chips/timestamps/wavelengths into the model.
          src_fr = inspect.getsource(fr)
          if 'family == "clay"' not in src_fr or "chips=" not in src_fr:
              fails.append("forward_router never routes chips/timestamps into Clay")
          if fails:
              print("PREFLIGHT FAILED — Clay path not ready:")
              for f in fails: print("  -", f)
              sys.exit(3)
          print("preflight OK")
          PY
          mkdir -p /cephfs/checkpoints/ladder/clay_r4
          cd /cephfs
          python /workspace/imintengine/scripts/train_unified.py \
            --backbone-name clay_v1_5 \
            --data-dirs /cephfs/unified_v2_512 \
            --label-dir /cephfs/nmd2023_distill_labels \
            --frac-dir /cephfs/tradslag_fracs \
            --enable-tradslag-head \
            --lambda-frac 1.0 \
            --checkpoint-dir /cephfs/checkpoints/ladder/clay_r4 \
            --num-classes 28 \
            --epochs 30 \
            --batch-size 8 \
            --lr 1e-4 \
            --weight-decay 0.35 \
            --loss-type focal_dice \
            --focal-gamma 2.0 \
            --patch-size 504 \
            --img-size 504 \
            --weighting-method sqrt \
            --label-smoothing 0.05 \
            --lovasz-weight 0.3 \
            --patience 20 \
            --num-workers 16 \
            --device cuda
          echo "=== Done: $(date) ==="
        volumeMounts:
        - { mountPath: /cephfs, name: cephfs }
        - { mountPath: /data, name: cephfs }
        - { mountPath: /dev/shm, name: dshm }
        resources:
          requests: { cpu: "16", memory: "48Gi", nvidia.com/gpu: "1" }
          limits:   { cpu: "16", memory: "48Gi", nvidia.com/gpu: "1" }
      volumes:
      - name: cephfs
        persistentVolumeClaim: { claimName: training-data-cephfs }
      - name: dshm
        emptyDir: { medium: Memory, sizeLimit: "16Gi" }
