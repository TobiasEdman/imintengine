# GENERATED by scripts/gen_ladder_manifests.py — do not edit.
# One-shot least-privilege migration: prepares crop_split plus
# isolated per-model head/record directories and the split record
# directory. Run before split; broad PVC roots stay unchanged.
# Plan: docs/experiments/ladder_distill_stage.md
apiVersion: batch/v1
kind: Job
metadata:
  name: ladder-crop-distill-storage-prep
  namespace: prithvi-training-default
  labels: { app: unified-training, purpose: ladder-crop-distill-storage }
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 600
  ttlSecondsAfterFinished: 172800
  template:
    metadata:
      labels: { app: unified-training, purpose: ladder-crop-distill-storage }
    spec:
      automountServiceAccountToken: false
      restartPolicy: Never
      imagePullSecrets:
        - name: ghcr-push
      securityContext:
        runAsUser: 0
        runAsGroup: 2000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: storage-prep
          image: ghcr.io/tobiasedman/imint-ladder-crop-distill@sha256:5d4ecf421953db7aff32a3e3a0f2451706a956368ddf7ab747af716218c1c572
          imagePullPolicy: IfNotPresent
          command:
            - /usr/local/bin/python
          args:
            - /opt/imintengine/scripts/prepare_crop_distill_storage.py
          env:
            - name: CROP_DISTILL_SOURCE_GIT_SHA
              value: "3f0fae93cd0fa8570cb848dd3ce5daedf34d0486"
            - name: CROP_DISTILL_IMAGE
              value: "ghcr.io/tobiasedman/imint-ladder-crop-distill@sha256:5d4ecf421953db7aff32a3e3a0f2451706a956368ddf7ab747af716218c1c572"
            - name: POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
              add: ["CHOWN", "FOWNER"]
            readOnlyRootFilesystem: true
            runAsUser: 0
            runAsGroup: 2000
          resources:
            requests: { cpu: "500m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "256Mi" }
          volumeMounts:
            - name: distill
              mountPath: /cephfs/distill
              subPath: distill
            - name: ops
              mountPath: /cephfs/ops
              subPath: ops
      volumes:
        - name: distill
          persistentVolumeClaim: { claimName: training-data-cephfs }
        - name: ops
          persistentVolumeClaim: { claimName: training-data-cephfs }
